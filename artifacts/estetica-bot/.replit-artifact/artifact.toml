kind = "web"
previewPath = "/"
title = "Estética Bot"
description = "Panel de gestión para una estética que organiza tratamientos, preguntas frecuentes, citas y un simulador de conversación para preparar la conexión con WhatsApp Business."
version = "1.0.0"
id = "artifacts/estetica-bot"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23006

[services.development]
run = "pnpm --filter @workspace/estetica-bot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/estetica-bot", "run", "build" ]
serve = "static"
publicDir = "artifacts/estetica-bot/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23006"
BASE_PATH = "/"
